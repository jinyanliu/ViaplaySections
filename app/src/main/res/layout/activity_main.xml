<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="se.sugarest.jane.viaplaysections.ui.MainActivity">

    <include layout="@layout/app_bar" />

    <!-- main content view -->
    <LinearLayout
        android:id="@+id/content_activity_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:orientation="vertical"
        android:padding="@dimen/activity_margin">

        <TextView
            android:id="@+id/section_title_label"
            style="@style/main_activity_section_title_label_style"
            android:text="@string/section_title_label" />

        <TextView
            android:id="@+id/section_title"
            style="@style/main_activity_section_description_label_style"
            tools:text="@string/section_long_title" />

        <TextView
            android:id="@+id/section_description_label"
            style="@style/main_activity_section_title_label_style"
            android:text="@string/section_description_label" />

        <TextView
            android:id="@+id/section_description"
            style="@style/main_activity_section_description_label_style"
            tools:text="@string/section_description" />
    </LinearLayout>

    <TextView
        android:visibility="invisible"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/empty_view"
        android:text="@string/text_message_empty_view"
        android:gravity="center"
        android:textSize="20sp"/>

    <!-- navigation drawer -->
    <android.support.v7.widget.RecyclerView
        android:id="@+id/left_drawer"
        android:layout_width="@dimen/drawer_menu_width"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="@color/colorPrimary"
        android:choiceMode="singleChoice"
        android:divider="@android:color/transparent"
        android:dividerHeight="0dp" />
</android.support.v4.widget.DrawerLayout>
